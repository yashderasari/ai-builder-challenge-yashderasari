{
  "name": "asset-tracking-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "Take-home challenge: lab equipment asset tracking, monorepo with hosted API and Next.js starter.",
  "license": "MIT",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "sharp",
      "unrs-resolver",
      "@zxing/browser",
      "bwip-js"
    ]
  }
}
